{
  "name": "gujarati-global",
  "version": "1.0.0",
  "private": true,
  "description": "Gujarati Global — diaspora community platform monorepo",
  "packageManager": "pnpm@9.1.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "eslint .",
    "migrate": "pnpm --filter @gujarati-global/api db:migrate",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\" --ignore-path .gitignore",
    "clean": "turbo run clean && rimraf node_modules",
    "smoke:prod": "pnpm --filter @gujarati-global/api exec tsx ../../scripts/smoke-test.ts"
  },
  "devDependencies": {
    "@gujarati-global/eslint-config": "workspace:*",
    "@gujarati-global/tsconfig": "workspace:*",
    "dotenv": "^17.4.2",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "rimraf": "^5.0.5",
    "turbo": "^2.0.6",
    "typescript": "^5.4.5"
  },
  "pnpm": {
    "overrides": {
      "typescript": "^5.4.5"
    }
  }
}
